<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">
    <title>
        <%= title %>
    </title>
    <% include layout/head %>

</head>

<body>
    <% include layout/nav %>
        <div class="container">
            <% if (messages) { %>
                <%- messages() %>
            <% } %>
            <!-- Blog Post -->
            <% if(posts) { %>
            <% posts.forEach(function(post){ %>
            <div class="card mb-4">
                <div class="card-body">
                    <h2 class="card-title"><%= post.title %></h2>
                    <p class="card-text"><%- truncateText(post.body, 400) %></p>
                    <a href="/posts/show/<%= post._id %>" class="btn btn-primary">Read More &rarr;</a>
                </div>
                <div class="card-footer text-muted">
                    Posted on <%= moment(post.date).format("DD-MM-YYYY") %> by
                    <a href="#"><%= post.author %></a> in <a href="#"><%= post.category %></a>
                </div>
            </div>
            <% }); %>
            <% } %>
            <br>
            <hr>

            <% include layout/footer %>
        </div>
        <!-- /container -->
</body>

</html>